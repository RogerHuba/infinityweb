# Django settings
SECRET_KEY=your-secret-key-here
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# CORS settings (set to True only for development)
CORS_ALLOW_ALL_ORIGINS=False

# Database settings (for production, configure PostgreSQL or other database)
# DATABASE_URL=postgres://user:password@localhost:5432/swg_infinity

# Add other environment variables as needed
EOF  
cd /home/project && cd swg-infinity-django && cat > .env.example << 'EOF'
# Django settings
SECRET_KEY=your-secret-key-here
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# CORS settings (set to True only for development)
CORS_ALLOW_ALL_ORIGINS=False

# Database settings (for production, configure PostgreSQL or other database)
# DATABASE_URL=postgres://user:password@localhost:5432/swg_infinity

# Add other environment variables as needed
